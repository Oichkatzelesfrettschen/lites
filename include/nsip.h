/*
 * NSIP (NS over IP encapsulation) configuration header
 * Xerox Network Systems (XNS) protocol tunneled over IP
 * Defines whether NSIP is compiled into the kernel
 */

#ifndef NSIP
#define NSIP 0
#endif
