/*
 * IMP (Interface Message Processor) configuration header
 * IMP was the ARPANET packet switching node
 * Defines whether IMP protocol is compiled into the kernel
 */

#ifndef IMP
#define IMP 0
#endif
