This directory organizes user space libraries for different Unix derivatives.
Subdirectories like *netbsd*, *illumos* and *haiku* each provide libc,
libpthread and librt implementations used by higher layers.
